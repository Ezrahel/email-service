apiVersion: v1
kind: Service
metadata:
  name: email-service-api
  namespace: email-service
  labels:
    app: email-service
    tier: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: email-service
    tier: api
